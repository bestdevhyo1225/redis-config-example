spring:
  profiles:
    active: test

  data:
    redis:
      mode: standalone
      nodes: localhost:6379
      command-timeout: 1000 # 1000ms
      shutdown-timeout: 100 # 100ms

logging:
  level:
    io.lettuce.core: debug
